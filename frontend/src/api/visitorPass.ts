import {request} from '../utils/request';import type{VisitorPass}from '../types';export interface CreateVisitorPassPayload{plate_no:string;building:string;unit:string;room:string;visit_start:string;visit_end:string}export const listVisitorPasses=(status?:string)=>request<VisitorPass[]>(`/visitor-passes${status?`?status=${status}`:''}`);export const createVisitorPass=(data:CreateVisitorPassPayload)=>request<VisitorPass>('/visitor-passes',{method:'POST',body:JSON.stringify(data)});export const approveVisitorPass=(id:number)=>request<VisitorPass>(`/visitor-passes/${id}/approve`,{method:'POST'});export const revokeVisitorPass=(id:number)=>request<VisitorPass>(`/visitor-passes/${id}/revoke`,{method:'POST'});
