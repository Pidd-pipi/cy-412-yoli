<template><el-tag :type="kind" effect="light">{{visitorPassStatusText[status]}}</el-tag></template><script setup lang="ts">import {computed}from'vue';import{visitorPassStatusText}from'../../constants/visitorPass';import type{VisitorPassStatus}from'../../types';const p=defineProps<{status:VisitorPassStatus}>();const kind=computed(()=>({pending:'warning',approved:'success',revoked:'info',expired:'danger'}[p.status] as 'success'))</script>
